<UserControl x:Class="LocalNEXUS.App.Views.Settings.LoopNodeSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:nodes="clr-namespace:LocalNEXUS.App.Nodes"
             d:DataContext="{d:DesignInstance Type=nodes:LoopNode}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <!--
        Nothing to configure and no fields invented to fill the space. What a loop is doing is the
        only question worth asking of it, so that is what the panel answers.
    -->

    <StackPanel>
        <TextBlock Style="{StaticResource HintText}"
                   TextWrapping="Wrap"
                   Text="Runs everything wired to the Item pin once for each entry in the list arriving on Text. Anything that is not a list counts as one item." />

        <TextBlock Style="{StaticResource HintText}"
                   Margin="0,8,0,0"
                   TextWrapping="Wrap"
                   Text="Put a breakpoint on the wire out of the Item pin to stop before every item and look at what is about to be processed. That is the one thing this node does that wiring a list straight into another node does not." />

        <TextBlock Margin="0,16,0,0"
                   FontWeight="SemiBold"
                   Text="{Binding ProgressText}" />

        <TextBlock Style="{StaticResource MonoText}"
                   Margin="0,6,0,0"
                   TextWrapping="Wrap"
                   Foreground="{DynamicResource Text.Secondary.Brush}"
                   Text="{Binding CurrentItem}" />
    </StackPanel>
</UserControl>
