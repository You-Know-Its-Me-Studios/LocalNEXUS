<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:sys="clr-namespace:System;assembly=System.Runtime"
                    xmlns:converters="clr-namespace:LocalNEXUS.App.Infrastructure.Converters">

    <!--
        Controls and type. Every colour in this file arrives as a brush from Brushes.xaml, which
        gets its colours from whichever theme dictionary is merged, so nothing here names one.

        Converters resolve brushes by formatting a value into a key: "Pin.{0}.Brush" plus a
        PinType of Code resolves Pin.Code.Brush. Adding a pin type, node state or activity kind
        therefore means adding a brush and nothing else.
    -->

    <converters:ResourceLookupConverter x:Key="ResourceLookup" />
    <converters:BrushKeyConverter x:Key="BrushKey" />
    <converters:EnumToBooleanConverter x:Key="EnumToBoolean" />
    <converters:EnumToVisibilityConverter x:Key="EnumToVisibility" />
    <converters:StringToVisibilityConverter x:Key="StringToVisibility" />
    <converters:CountToVisibilityConverter x:Key="CountToVisibility" />
    <converters:NullToVisibilityConverter x:Key="NullToVisibility" />
    <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibility" />
    <converters:BooleanToInverseConverter x:Key="BooleanToInverse" />

    <!--
        One type scale, used everywhere. Five steps is enough for an interface this dense and
        few enough that a sixth is always a mistake rather than a need.
    -->
    <sys:Double x:Key="Font.Size.Micro">10.5</sys:Double>
    <sys:Double x:Key="Font.Size.Small">11.5</sys:Double>
    <sys:Double x:Key="Font.Size.Normal">12.5</sys:Double>
    <sys:Double x:Key="Font.Size.Large">14</sys:Double>
    <sys:Double x:Key="Font.Size.Title">16</sys:Double>

    <FontFamily x:Key="Font.Sans">Segoe UI Variable Text, Segoe UI, Arial</FontFamily>

    <!--
        Icons. Not bundled, unlike the monospace face: these ship with Windows 11, which is the
        only platform this runs on, and a glyph font is a poor thing to carry a copy of when the
        operating system already guarantees one.
    -->
    <FontFamily x:Key="Font.Icons">Segoe Fluent Icons, Segoe MDL2 Assets</FontFamily>

    <!--
        JetBrains Mono travels with the application as a compiled resource, so paths, identifiers
        and diagnostics look the same on every machine rather than falling to whatever monospace
        the machine happens to have. The names after it are the fallback chain if the resource
        cannot be loaded at all, and they are ordinary installed fonts rather than a second copy.
    -->
    <FontFamily x:Key="Font.Mono">pack://application:,,,/Assets/Fonts/#JetBrains Mono, Cascadia Mono, Consolas, Courier New</FontFamily>

    <!-- Base text -->
    <!--
        The base text style deliberately does not set a font family. The window sets the sans face
        and every text block inherits it, and an implicit style setter would beat that inheritance:
        a button whose style asks for the icon face would still render its glyph in the sans face,
        which is how an icon becomes an empty box.
    -->
    <Style TargetType="TextBlock">
        <Setter Property="Foreground" Value="{DynamicResource Text.Primary.Brush}" />
        <Setter Property="FontSize" Value="{StaticResource Font.Size.Normal}" />
        <Setter Property="TextOptions.TextFormattingMode" Value="Ideal" />
    </Style>

    <!-- Anything that is a path, an identifier, a diagnostic or a number to be compared. -->
    <Style x:Key="MonoText" TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
        <Setter Property="FontFamily" Value="{StaticResource Font.Mono}" />
        <Setter Property="FontSize" Value="{StaticResource Font.Size.Small}" />
    </Style>

    <Style x:Key="MutedText" TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
        <Setter Property="Foreground" Value="{DynamicResource Text.Muted.Brush}" />
        <Setter Property="FontSize" Value="{StaticResource Font.Size.Small}" />
    </Style>

    <Style x:Key="FieldLabel" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{DynamicResource Text.Secondary.Brush}" />
        <Setter Property="FontSize" Value="{StaticResource Font.Size.Small}" />
        <Setter Property="Margin" Value="0,10,0,4" />
    </Style>

    <Style x:Key="SectionHeader" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{DynamicResource Text.Primary.Brush}" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Margin" Value="0,0,0,2" />
    </Style>

    <Style x:Key="HintText" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{DynamicResource Text.Secondary.Brush}" />
        <Setter Property="FontSize" Value="{StaticResource Font.Size.Small}" />
        <Setter Property="TextWrapping" Value="Wrap" />
        <Setter Property="Margin" Value="0,4,0,0" />
    </Style>

    <!-- Text entry -->
    <Style TargetType="TextBox">
        <Setter Property="Background" Value="{DynamicResource Surface.Input.Brush}" />
        <Setter Property="Foreground" Value="{DynamicResource Text.Primary.Brush}" />
        <Setter Property="CaretBrush" Value="{DynamicResource Text.Primary.Brush}" />
        <Setter Property="SelectionBrush" Value="{DynamicResource Accent.Primary.Brush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Surface.Border.Brush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="6,4" />
        <Setter Property="FontSize" Value="{StaticResource Font.Size.Normal}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border x:Name="Root"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="4">
                        <ScrollViewer x:Name="PART_ContentHost"
                                      Margin="{TemplateBinding Padding}"
                                      VerticalScrollBarVisibility="{TemplateBinding VerticalScrollBarVisibility}"
                                      Focusable="False" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsKeyboardFocusWithin" Value="True">
                            <Setter TargetName="Root" Property="BorderBrush" Value="{DynamicResource Accent.Primary.Brush}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="MonoTextBox" TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
        <Setter Property="FontFamily" Value="{StaticResource Font.Mono}" />
        <Setter Property="AcceptsReturn" Value="True" />
        <Setter Property="TextWrapping" Value="Wrap" />
        <Setter Property="VerticalScrollBarVisibility" Value="Auto" />
        <Setter Property="SpellCheck.IsEnabled" Value="False" />
    </Style>

    <!-- Buttons -->
    <Style TargetType="Button">
        <Setter Property="Background" Value="{DynamicResource Surface.Card.Brush}" />
        <Setter Property="Foreground" Value="{DynamicResource Text.Primary.Brush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Surface.Border.Brush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="12,6" />
        <Setter Property="FontSize" Value="{StaticResource Font.Size.Normal}" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Root"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="4">
                        <ContentPresenter Margin="{TemplateBinding Padding}"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Root" Property="BorderBrush" Value="{DynamicResource Accent.Primary.Brush}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Root" Property="Background" Value="{DynamicResource Surface.Input.Brush}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.4" />
                            <Setter Property="Cursor" Value="Arrow" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
        <Setter Property="Background" Value="{DynamicResource Accent.Primary.Brush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Accent.Primary.Brush}" />
        <Setter Property="Foreground" Value="{DynamicResource Text.Inverse.Brush}" />
        <Setter Property="FontWeight" Value="SemiBold" />
    </Style>

    <Style x:Key="SmallButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
        <Setter Property="Padding" Value="8,3" />
        <Setter Property="FontSize" Value="{StaticResource Font.Size.Small}" />
    </Style>

    <!-- Check boxes and radio buttons -->
    <Style TargetType="CheckBox">
        <Setter Property="Foreground" Value="{DynamicResource Text.Primary.Brush}" />
        <Setter Property="FontSize" Value="{StaticResource Font.Size.Normal}" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <StackPanel Orientation="Horizontal" Background="Transparent">
                        <Border x:Name="Box"
                                Width="15" Height="15"
                                CornerRadius="3"
                                VerticalAlignment="Center"
                                Background="{DynamicResource Surface.Input.Brush}"
                                BorderBrush="{DynamicResource Surface.Border.Brush}"
                                BorderThickness="1">
                            <Path x:Name="Tick"
                                  Data="M 2,6 L 5.5,10 L 12,2.5"
                                  Stroke="{DynamicResource Accent.Primary.Brush}"
                                  StrokeThickness="2"
                                  Visibility="Collapsed"
                                  Stretch="None" />
                        </Border>
                        <ContentPresenter Margin="7,0,0,0" VerticalAlignment="Center" />
                    </StackPanel>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Tick" Property="Visibility" Value="Visible" />
                            <Setter TargetName="Box" Property="BorderBrush" Value="{DynamicResource Accent.Primary.Brush}" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Box" Property="BorderBrush" Value="{DynamicResource Accent.Primary.Brush}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.45" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="RadioButton">
        <Setter Property="Foreground" Value="{DynamicResource Text.Primary.Brush}" />
        <Setter Property="FontSize" Value="{StaticResource Font.Size.Normal}" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Margin" Value="0,0,14,0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RadioButton">
                    <StackPanel Orientation="Horizontal" Background="Transparent">
                        <Border x:Name="Ring"
                                Width="15" Height="15"
                                CornerRadius="8"
                                VerticalAlignment="Center"
                                Background="{DynamicResource Surface.Input.Brush}"
                                BorderBrush="{DynamicResource Surface.Border.Brush}"
                                BorderThickness="1">
                            <Ellipse x:Name="Dot"
                                     Width="7" Height="7"
                                     Fill="{DynamicResource Accent.Primary.Brush}"
                                     Visibility="Collapsed" />
                        </Border>
                        <ContentPresenter Margin="7,0,0,0" VerticalAlignment="Center" />
                    </StackPanel>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Dot" Property="Visibility" Value="Visible" />
                            <Setter TargetName="Ring" Property="BorderBrush" Value="{DynamicResource Accent.Primary.Brush}" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Ring" Property="BorderBrush" Value="{DynamicResource Accent.Primary.Brush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Combo box, retemplated so the popup matches the rest of the window -->
    <Style x:Key="ComboToggle" TargetType="ToggleButton">
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Border x:Name="Root"
                            Background="{DynamicResource Surface.Input.Brush}"
                            BorderBrush="{DynamicResource Surface.Border.Brush}"
                            BorderThickness="1"
                            CornerRadius="4">
                        <Path x:Name="Arrow"
                              HorizontalAlignment="Right"
                              VerticalAlignment="Center"
                              Margin="0,0,9,0"
                              Data="M 0,0 L 8,0 L 4,5 Z"
                              Fill="{DynamicResource Text.Secondary.Brush}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Root" Property="BorderBrush" Value="{DynamicResource Accent.Primary.Brush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ComboBoxItem">
        <Setter Property="Foreground" Value="{DynamicResource Text.Primary.Brush}" />
        <Setter Property="Padding" Value="8,5" />
        <Setter Property="FontSize" Value="{StaticResource Font.Size.Normal}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBoxItem">
                    <Border x:Name="Root" Background="Transparent" Padding="{TemplateBinding Padding}">
                        <ContentPresenter />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Root" Property="Background" Value="{DynamicResource Surface.Card.Brush}" />
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Root" Property="Background" Value="{DynamicResource Accent.Primary.Brush}" />
                            <Setter Property="Foreground" Value="{DynamicResource Text.Inverse.Brush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ComboBox">
        <Setter Property="Foreground" Value="{DynamicResource Text.Primary.Brush}" />
        <Setter Property="FontSize" Value="{StaticResource Font.Size.Normal}" />
        <Setter Property="Padding" Value="8,5" />
        <Setter Property="MinHeight" Value="26" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid>
                        <ToggleButton Style="{StaticResource ComboToggle}"
                                      Focusable="False"
                                      IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                      ClickMode="Press" />
                        <ContentPresenter x:Name="Selection"
                                          Margin="{TemplateBinding Padding}"
                                          HorizontalAlignment="Left"
                                          VerticalAlignment="Center"
                                          IsHitTestVisible="False"
                                          Content="{TemplateBinding SelectionBoxItem}"
                                          ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                          ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}" />
                        <!--
                            The editable case. A ComboBox with IsEditable set puts its text in a
                            TextBox the template has to supply and named PART_EditableTextBox, and
                            leaves SelectionBoxItem empty. Without one the framework has nowhere to
                            put the text, so the box renders blank and cannot be typed into, which
                            is exactly how it behaved.

                            Its own background, border and padding are set here so the TextBox
                            style does not draw a second input inside this one. Everything it is
                            painted with still comes from that style.
                        -->
                        <TextBox x:Name="PART_EditableTextBox"
                                 Margin="8,0,26,0"
                                 VerticalAlignment="Center"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 Padding="0"
                                 Visibility="Collapsed"
                                 IsReadOnly="{TemplateBinding IsReadOnly}" />

                        <TextBlock x:Name="Placeholder"
                                   Margin="{TemplateBinding Padding}"
                                   VerticalAlignment="Center"
                                   Foreground="{DynamicResource Text.Secondary.Brush}"
                                   Text="nothing to choose"
                                   Visibility="Collapsed"
                                   IsHitTestVisible="False" />
                        <Popup x:Name="PART_Popup"
                               Placement="Bottom"
                               AllowsTransparency="True"
                               Focusable="False"
                               PopupAnimation="Fade"
                               IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}">
                            <Border Background="{DynamicResource Surface.Panel.Brush}"
                                    BorderBrush="{DynamicResource Surface.Border.Brush}"
                                    BorderThickness="1"
                                    CornerRadius="4"
                                    MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}"
                                    MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                <ScrollViewer>
                                    <ItemsPresenter />
                                </ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEditable" Value="True">
                            <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible" />
                            <Setter TargetName="Selection" Property="Visibility" Value="Collapsed" />
                        </Trigger>

                        <!--
                            Nothing to choose is about the list, and an editable box with an empty
                            list is still perfectly usable because anything can be typed into it.
                            Saying otherwise over the top of what somebody is typing would be
                            wrong twice.
                        -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="HasItems" Value="False" />
                                <Condition Property="IsEditable" Value="False" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="Placeholder" Property="Visibility" Value="Visible" />
                        </MultiTrigger>

                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Menus. The system template paints a white popup with system colours, so every role
         gets a dark template of its own. -->
    <Style TargetType="Menu">
        <Setter Property="Background" Value="{DynamicResource Surface.Panel.Brush}" />
        <Setter Property="Foreground" Value="{DynamicResource Text.Primary.Brush}" />
        <Setter Property="FontSize" Value="{StaticResource Font.Size.Normal}" />
        <Setter Property="Padding" Value="4,2" />
    </Style>

    <Style TargetType="Separator">
        <Setter Property="Background" Value="{DynamicResource Surface.Border.Brush}" />
        <Setter Property="Height" Value="1" />
        <Setter Property="Margin" Value="0,3" />
    </Style>

    <Style x:Key="{x:Static MenuItem.SeparatorStyleKey}" TargetType="Separator">
        <Setter Property="Background" Value="{DynamicResource Surface.Border.Brush}" />
        <Setter Property="Height" Value="1" />
        <Setter Property="Margin" Value="8,4" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Separator">
                    <Rectangle Height="1" Fill="{TemplateBinding Background}" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <ControlTemplate x:Key="MenuItem.TopLevelHeaderTemplate" TargetType="MenuItem">
        <Border x:Name="Root" Background="Transparent" CornerRadius="4" Padding="10,5">
            <Grid>
                <ContentPresenter ContentSource="Header"
                                  RecognizesAccessKey="True"
                                  VerticalAlignment="Center" />
                <Popup x:Name="PART_Popup"
                       Placement="Bottom"
                       VerticalOffset="4"
                       IsOpen="{TemplateBinding IsSubmenuOpen}"
                       AllowsTransparency="True"
                       Focusable="False"
                       PopupAnimation="Fade">
                    <Border Background="{DynamicResource Surface.Card.Brush}"
                            BorderBrush="{DynamicResource Surface.Border.Brush}"
                            BorderThickness="1"
                            CornerRadius="6"
                            Padding="4"
                            MinWidth="240">
                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Cycle" />
                    </Border>
                </Popup>
            </Grid>
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="IsHighlighted" Value="True">
                <Setter TargetName="Root" Property="Background" Value="{DynamicResource Surface.Card.Brush}" />
            </Trigger>
            <Trigger Property="IsSubmenuOpen" Value="True">
                <Setter TargetName="Root" Property="Background" Value="{DynamicResource Surface.Card.Brush}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.4" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <ControlTemplate x:Key="MenuItem.TopLevelItemTemplate" TargetType="MenuItem">
        <Border x:Name="Root" Background="Transparent" CornerRadius="4" Padding="10,5">
            <ContentPresenter ContentSource="Header"
                              RecognizesAccessKey="True"
                              VerticalAlignment="Center" />
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="IsHighlighted" Value="True">
                <Setter TargetName="Root" Property="Background" Value="{DynamicResource Surface.Card.Brush}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.4" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <ControlTemplate x:Key="MenuItem.SubmenuItemTemplate" TargetType="MenuItem">
        <Border x:Name="Root" Background="Transparent" CornerRadius="4" Padding="10,6" Margin="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <ContentPresenter Grid.Column="0"
                                  ContentSource="Header"
                                  RecognizesAccessKey="True"
                                  VerticalAlignment="Center" />
                <TextBlock Grid.Column="1"
                           Text="{TemplateBinding InputGestureText}"
                           Margin="28,0,4,0"
                           VerticalAlignment="Center"
                           FontSize="{StaticResource Font.Size.Small}"
                           Foreground="{DynamicResource Text.Secondary.Brush}" />
            </Grid>
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="IsHighlighted" Value="True">
                <Setter TargetName="Root" Property="Background" Value="{DynamicResource Surface.Input.Brush}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.4" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <ControlTemplate x:Key="MenuItem.SubmenuHeaderTemplate" TargetType="MenuItem">
        <Border x:Name="Root" Background="Transparent" CornerRadius="4" Padding="10,6" Margin="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <ContentPresenter Grid.Column="0"
                                  ContentSource="Header"
                                  RecognizesAccessKey="True"
                                  VerticalAlignment="Center" />
                <Path Grid.Column="1"
                      Data="M 0 0 L 4 4 L 0 8 Z"
                      Fill="{DynamicResource Text.Secondary.Brush}"
                      Margin="28,0,4,0"
                      VerticalAlignment="Center" />
                <Popup x:Name="PART_Popup"
                       Placement="Right"
                       HorizontalOffset="6"
                       IsOpen="{TemplateBinding IsSubmenuOpen}"
                       AllowsTransparency="True"
                       Focusable="False"
                       PopupAnimation="Fade">
                    <Border Background="{DynamicResource Surface.Card.Brush}"
                            BorderBrush="{DynamicResource Surface.Border.Brush}"
                            BorderThickness="1"
                            CornerRadius="6"
                            Padding="4"
                            MinWidth="200">
                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Cycle" />
                    </Border>
                </Popup>
            </Grid>
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="IsHighlighted" Value="True">
                <Setter TargetName="Root" Property="Background" Value="{DynamicResource Surface.Input.Brush}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.4" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style TargetType="MenuItem">
        <Setter Property="Foreground" Value="{DynamicResource Text.Primary.Brush}" />
        <Setter Property="FontSize" Value="{StaticResource Font.Size.Normal}" />
        <Setter Property="Template" Value="{StaticResource MenuItem.SubmenuItemTemplate}" />
        <Style.Triggers>
            <Trigger Property="Role" Value="TopLevelHeader">
                <Setter Property="Template" Value="{StaticResource MenuItem.TopLevelHeaderTemplate}" />
            </Trigger>
            <Trigger Property="Role" Value="TopLevelItem">
                <Setter Property="Template" Value="{StaticResource MenuItem.TopLevelItemTemplate}" />
            </Trigger>
            <Trigger Property="Role" Value="SubmenuHeader">
                <Setter Property="Template" Value="{StaticResource MenuItem.SubmenuHeaderTemplate}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Top level tabs. A flat strip with an accent underline on the active tab. -->
    <Style TargetType="TabControl">
        <Setter Property="Background" Value="{DynamicResource Surface.Window.Brush}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabControl">
                    <DockPanel>
                        <Border DockPanel.Dock="Top"
                                Background="{DynamicResource Surface.Panel.Brush}"
                                BorderBrush="{DynamicResource Surface.Border.Brush}"
                                BorderThickness="0,0,0,1">
                            <TabPanel IsItemsHost="True" Margin="8,0,8,0" />
                        </Border>
                        <ContentPresenter ContentSource="SelectedContent" />
                    </DockPanel>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="TabItem">
        <Setter Property="Foreground" Value="{DynamicResource Text.Secondary.Brush}" />
        <Setter Property="FontSize" Value="{StaticResource Font.Size.Large}" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabItem">
                    <Border Background="Transparent" Padding="16,9,16,7">
                        <StackPanel>
                            <ContentPresenter ContentSource="Header" />
                            <Rectangle x:Name="Underline"
                                       Height="2"
                                       Margin="0,6,0,0"
                                       RadiusX="1"
                                       RadiusY="1"
                                       Fill="Transparent" />
                        </StackPanel>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Foreground" Value="{DynamicResource Text.Primary.Brush}" />
                            <Setter TargetName="Underline" Property="Fill" Value="{DynamicResource Accent.Primary.Brush}" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Foreground" Value="{DynamicResource Text.Primary.Brush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Selection lists. The system template is a white box; the network tab's model list
         wants rounded cards with an accent border on the selected row. -->
    <Style TargetType="ListBox">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
    </Style>

    <Style TargetType="ListBoxItem">
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                    <Border x:Name="Root"
                            Background="{DynamicResource Surface.Card.Brush}"
                            BorderBrush="{DynamicResource Surface.Border.Brush}"
                            BorderThickness="1"
                            CornerRadius="8"
                            Margin="0,0,0,10"
                            Padding="16,12">
                        <ContentPresenter />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Root" Property="BorderBrush" Value="{DynamicResource Accent.Neutral.Brush}" />
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Root" Property="BorderBrush" Value="{DynamicResource Accent.Primary.Brush}" />
                            <Setter TargetName="Root" Property="Background" Value="{DynamicResource Surface.Input.Brush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Panels -->
    <Style x:Key="Card" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource Surface.Card.Brush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Surface.Border.Brush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="6" />
        <Setter Property="Padding" Value="12" />
    </Style>

    <!--
        Scroll bars. The system ones are painted with system colours, which is how a dark window
        ends up with a light grey gutter down the side of every panel. Thin, flat, and part of the
        theme like everything else.
    -->
    <Style x:Key="ScrollBar.Thumb" TargetType="Thumb">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Thumb">
                    <Border x:Name="Root"
                            Background="{DynamicResource Accent.Neutral.Brush}"
                            CornerRadius="4"
                            Margin="2"
                            Opacity="0.45" />
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Root" Property="Opacity" Value="0.8" />
                        </Trigger>
                        <Trigger Property="IsDragging" Value="True">
                            <Setter TargetName="Root" Property="Opacity" Value="1" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- The arrow buttons and the page track are removed rather than restyled: nothing needs them. -->
    <Style x:Key="ScrollBar.HiddenPart" TargetType="RepeatButton">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RepeatButton">
                    <Border Background="Transparent" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ScrollBar">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Width" Value="11" />
        <Setter Property="MinWidth" Value="11" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ScrollBar">
                    <Grid Background="{TemplateBinding Background}">
                        <Track x:Name="PART_Track" IsDirectionReversed="True">
                            <Track.DecreaseRepeatButton>
                                <RepeatButton Style="{StaticResource ScrollBar.HiddenPart}" Command="ScrollBar.PageUpCommand" />
                            </Track.DecreaseRepeatButton>
                            <Track.Thumb>
                                <Thumb Style="{StaticResource ScrollBar.Thumb}" />
                            </Track.Thumb>
                            <Track.IncreaseRepeatButton>
                                <RepeatButton Style="{StaticResource ScrollBar.HiddenPart}" Command="ScrollBar.PageDownCommand" />
                            </Track.IncreaseRepeatButton>
                        </Track>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="Orientation" Value="Horizontal">
                            <Setter Property="Height" Value="11" />
                            <Setter Property="MinHeight" Value="11" />
                            <Setter Property="Width" Value="Auto" />
                            <Setter Property="MinWidth" Value="0" />
                            <Setter TargetName="PART_Track" Property="IsDirectionReversed" Value="False" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Tool tips. The system template is a white box with a black border in every theme. -->
    <Style TargetType="ToolTip">
        <Setter Property="Foreground" Value="{DynamicResource Text.Primary.Brush}" />
        <Setter Property="FontFamily" Value="{StaticResource Font.Sans}" />
        <Setter Property="FontSize" Value="{StaticResource Font.Size.Small}" />
        <Setter Property="MaxWidth" Value="380" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToolTip">
                    <Border Background="{DynamicResource Surface.Card.Brush}"
                            BorderBrush="{DynamicResource Surface.Border.Brush}"
                            BorderThickness="1"
                            CornerRadius="4"
                            Padding="9,6">
                        <ContentPresenter />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Right click menus, which reuse the submenu item template the menu bar already defines. -->
    <Style TargetType="ContextMenu">
        <Setter Property="Foreground" Value="{DynamicResource Text.Primary.Brush}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ContextMenu">
                    <Border Background="{DynamicResource Surface.Card.Brush}"
                            BorderBrush="{DynamicResource Surface.Border.Brush}"
                            BorderThickness="1"
                            CornerRadius="6"
                            Padding="4">
                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Cycle" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--
        Progress. Used by a node that can say how far through a list of files it is, and by the
        Python environment while it downloads.
    -->
    <Style TargetType="ProgressBar">
        <Setter Property="Height" Value="3" />
        <Setter Property="Foreground" Value="{DynamicResource Accent.Primary.Brush}" />
        <Setter Property="Background" Value="{DynamicResource Surface.Track.Brush}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ProgressBar">
                    <Border Background="{TemplateBinding Background}" CornerRadius="2" ClipToBounds="True">
                        <Grid>
                            <Rectangle x:Name="PART_Track" />
                            <Rectangle x:Name="PART_Indicator"
                                       HorizontalAlignment="Left"
                                       Fill="{TemplateBinding Foreground}"
                                       RadiusX="2"
                                       RadiusY="2" />
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--
        Sliders. The filled part of the track is what has been committed and the rest is what is
        still available, so the control reads as a proportion of something rather than as a
        position on a line. Ticks are drawn under it and are markers only; snapping is off, so a
        tick shows where a meaningful value sits without stopping the thumb anywhere else.
    -->
    <Style x:Key="Slider.Thumb" TargetType="Thumb">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Thumb">
                    <Border x:Name="Root"
                            Width="13"
                            Height="13"
                            CornerRadius="7"
                            Background="{DynamicResource Accent.Primary.Brush}"
                            BorderBrush="{DynamicResource Surface.Window.Brush}"
                            BorderThickness="2" />
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Root" Property="Width" Value="15" />
                            <Setter TargetName="Root" Property="Height" Value="15" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Root" Property="Background" Value="{DynamicResource Accent.Neutral.Brush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="Slider">
        <Setter Property="Foreground" Value="{DynamicResource Accent.Primary.Brush}" />
        <Setter Property="Background" Value="{DynamicResource Surface.Track.Brush}" />
        <Setter Property="MinHeight" Value="26" />
        <Setter Property="IsSnapToTickEnabled" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Slider">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <Border Grid.Row="0"
                                Height="4"
                                CornerRadius="2"
                                VerticalAlignment="Center"
                                Background="{TemplateBinding Background}" />

                        <Track Grid.Row="0" x:Name="PART_Track">
                            <Track.DecreaseRepeatButton>
                                <RepeatButton Command="Slider.DecreaseLarge">
                                    <RepeatButton.Template>
                                        <ControlTemplate TargetType="RepeatButton">
                                            <Border Height="4"
                                                    CornerRadius="2"
                                                    VerticalAlignment="Center"
                                                    Background="{DynamicResource Accent.Primary.Brush}" />
                                        </ControlTemplate>
                                    </RepeatButton.Template>
                                </RepeatButton>
                            </Track.DecreaseRepeatButton>

                            <Track.Thumb>
                                <Thumb Style="{StaticResource Slider.Thumb}" />
                            </Track.Thumb>

                            <Track.IncreaseRepeatButton>
                                <RepeatButton Command="Slider.IncreaseLarge">
                                    <RepeatButton.Template>
                                        <ControlTemplate TargetType="RepeatButton">
                                            <Border Background="Transparent" />
                                        </ControlTemplate>
                                    </RepeatButton.Template>
                                </RepeatButton>
                            </Track.IncreaseRepeatButton>
                        </Track>

                        <!-- Draws nothing when there are no ticks, so it needs no hiding. -->
                        <TickBar Grid.Row="1"
                                 Height="4"
                                 Placement="Bottom"
                                 Fill="{DynamicResource Text.Muted.Brush}"
                                 Ticks="{TemplateBinding Ticks}"
                                 Minimum="{TemplateBinding Minimum}"
                                 Maximum="{TemplateBinding Maximum}" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--
        Splitters. A hairline in the border colour inside a wider invisible grab area, so the
        divider reads as a rule rather than as a bar.
    -->
    <Style TargetType="GridSplitter">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="GridSplitter">
                    <Grid Background="Transparent">
                        <Rectangle x:Name="Line" Fill="{DynamicResource Surface.Border.Brush}" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Line" Property="Fill" Value="{DynamicResource Accent.Primary.Brush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
